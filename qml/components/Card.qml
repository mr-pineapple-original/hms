import QtQuick 2.15
import hms

Rectangle {
    width: 420
    height: 380

    radius: Theme.radius
    color: Theme.card

    anchors.centerIn: parent

    border.color: Theme.border
    border.width: 1
}